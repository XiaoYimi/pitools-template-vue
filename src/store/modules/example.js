// modules state
const state = []

// getters: It is equivalent to computed of those components.  You can use 'rootState' to get the root state of the store
const getters = {}

// mutations
const mutations = {}

// actionc
const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actionc
}